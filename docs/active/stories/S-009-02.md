---
id: S-009-02
title: restore-drifted-surfaces-to-standard
type: story
status: open
priority: high
tickets: [T-009-02-01, T-009-02-02, T-009-02-03]
---

**Scope:** The drifted user-facing surfaces conformed to the S-009-01 standard: src/pages/backstage.astro (the intro .note passcode lecture, the .section-copy paragraphs, the title/description meta, and its script's user-facing strings) and src/pages/index.astro (tagline, ledes, receipt copy, meta, and its script strings). Playwright specs tests/backstage-flow.spec.ts and tests/demo-flow.spec.ts (+ tests/support/flow-contract.ts) are kept green where copy-coupled. BaseLayout carries no literal copy of its own (it renders title/description props) — audited, not edited.

**Story acceptance:** Reading each rewritten surface cold at projector distance and on a phone, every string is brief, plain, and half-second-legible — no two-paragraph passcode lecture, no insider self-reference, no descriptive-sentence title — while the P3 — the room and phone both work safely safety meaning (don't paste secrets here) survives in compressed form; `npx playwright test` passes end to end; the leak check still passes with no passcode, secret, or template-development history on public copy; and a docs/active/work cold-read artifact answers 'what is this, what do I do' in half a second from screenshots alone.

**Honest boundary:** The cold-read pass is the implementing session's own proxy; the true never-seen-it human read is the epic's human gate at the counter, deferred there, not simulated by a check (N4 — not automated theater). Template slots stay placeholders — a generated project's real DEMO_NAME/PRIMARY_ACTION_LABEL come from its idea (N1 — not a universal application framework), conformed to shape here, not pre-written. No automated voice gate is added (that boundary is held in S-009-01).

## DAG

```
T-009-02-01  rewrite-backstage-copy-to-standard  ← T-009-01-01, T-009-01-02
T-009-02-02  rewrite-index-copy-to-standard  ← T-009-01-01, T-009-01-02
T-009-02-03  cold-read-and-flow-verification  ← T-009-02-01, T-009-02-02
```

Wave rationale: Both rewrite tickets depend on the settled standard AND its live wiring from S-009-01, so each sweep session actually reads the standard it conforms to — the cheapest real proof the authoring read-path works. They then fan out in parallel: backstage.astro and index.astro are disjoint files with no shared state, each keeping its own Playwright spec green. T-009-02-03 runs last, after both surfaces stop moving, because the combined cold-read, leak, and flow proof needs the final copy.

**Out of this slice:** Any change to backstage auth, storage, or the receipt API (strings only; DOM hooks and handlers untouched); restyling the clay kit or tokens; per-demo copywriting of a generated project's real name/action; building the fleet-wide voice gate; touching src/lib/* or the session worker.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-11._
