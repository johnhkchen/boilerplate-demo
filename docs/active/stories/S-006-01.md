---
id: S-006-01
title: assembly-playbook-authoring
type: story
status: open
priority: high
tickets: [T-006-01-01, T-006-01-02]
---

**Scope:** docs/knowledge/assembly-playbook.md (new) plus a sample sponsor packet under test/fixtures/sponsor-packet/. The playbook composes the existing knowledge docs — charter, product-spec, deployment, integration-check, backstage-retrieval-seam, vend-workflow — by reference into one ordered Day-1 sequence. No src/** changes, no new scripts or tooling.

**Story acceptance:** docs/knowledge/assembly-playbook.md exists and every check, script, command, or seam it names resolves to a real path in this repo (integration-check, ops-check, backstage feed, deployment steps); its mandatory exit condition is the core moment observed working under the harness's time budgets followed by conversion of every deferral into a one-line demand signal; and a fixture sponsor packet supplies one artifact per input class the playbook's intake step names, with leak-check clean over the fixture.

**Honest boundary:** Authored and fixture-proven only — the live dry run is S-006-02, named there, not smuggled here. No provider-specific recipes: per N2 — not an all-provider abstraction layer those wait for repeated evidence across events. The sponsor packet uses a fake sponsor API in the spirit of the harness's fake slow integration; no real credentials enter the repo.

## DAG

```
T-006-01-01  author-assembly-playbook
T-006-01-02  sample-sponsor-packet-fixture  ← T-006-01-01
```

Wave rationale: T-006-01-01 runs alone first: the fixture packet must mirror the playbook's declared intake shape, so T-006-01-02 waits on the settled intake list rather than guessing it. The two touch disjoint paths (docs/knowledge vs test/fixtures), so the sequencing is about content shape, not merge safety.

**Out of this slice:** Running the rehearsal (S-006-02); Day-2 productization and handoff-rehearsal playbooks (demand signals #2 and #3, their own epics); any automation or tooling around the playbook (epic out-of-scope); edits to src/** or to the checks the playbook references.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-11._
