---
id: S-010-03
title: flow-reads-contract
type: story
status: open
priority: high
tickets: [T-010-03-01]
---

**Scope:** The Playwright flow slice — tests/demo-flow.spec.ts (read the heading, status/body/evidence selectors + patterns, primary-action name, and the stalled-route glob from the declaration instead of the Demo Runway / #receipt-* / **/api/receipt literals), tests/support/flow-contract.ts (re-export the receipt boundary's landmark from boundary-contract.ts so there is one source), and playwright.config.ts only if the route/base wiring needs it (base URL is already env-driven).

**Story acceptance:** npm run test:flow (healthy) and npm run test:flow:stalled pass on the receipt boundary; the spec contains no literal Demo Runway, #receipt-nonce/#receipt-signature/#receipt-body/#receipt-status, or **/api/receipt — each resolves from the contract; and PRIMARY_ACTION_NAME plus the landmark heading have a single definition (in the contract), evidenced by the healthy flow still finding the labeled action.

**Honest boundary:** The flow still runs only against the RECEIPT boundary here; proving a genuine swap (a different heading + differently-shaped page) drives green/red is S-010-04. The backstage phone flow, its D1 migration, and the shared passcode are untouched — this story makes only the receipt healthy/stalled flow generic. Fixture-proven against the local dev server, FREE.

## DAG

```
T-010-03-01  flow-reads-declared-landmark  ← T-010-01-01
```

Wave rationale: One ticket. It shares no files with the Node harness story, so it fans out in parallel with the whole S-010-02 chain; both depend only on the settled declaration from S-010-01.

**Out of this slice:** The Node scripts and cores (S-010-02); the backstage flow, its migration, or the shared passcode; the end-to-end swap + npm run verify (S-010-04); adding a second real page.

---
_Materialized by Vend's `decompose-epic` play — 1 ticket(s), 2026-07-12._
