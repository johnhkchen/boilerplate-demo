---
id: S-010-04
title: swap-proof-and-verify
type: story
status: open
priority: high
tickets: [T-010-04-01, T-010-04-02]
---

**Scope:** The cross-harness acceptance — a throwaway alternate boundary declaration (different path, differently-shaped signed response, different heading/selectors) plus a stub server/page fixture, exercised through the Node checks and the Playwright flow BY DECLARATION ONLY, and a final npm run verify on the reverted receipt boundary. Touches test fixtures and a swap-proof harness; edits no check script or flow spec (that they need no edit is the point).

**Story acceptance:** With only the alternate declaration swapped in (no edits to scripts/*, the cores, or demo-flow.spec.ts), the checks pass when the alternate boundary is healthy and fail explicitly when it is broken, stalled, or leaking; omitting/deleting the declared route makes the harness fail loudly with a missing/unsatisfied-contract error rather than passing green; and after reverting to receiptBoundary, npm run verify passes end to end.

**Honest boundary:** The alternate boundary is a THROWAWAY test fixture (stub server + minimal page), not a second shipped boundary — the template still ships only the receipt (out-of-scope 'adding new boundaries' respected). The swap proof runs locally/fixture-metered, FREE; it does not judge whether either demo is convincing (N4 — not automated theater) — only that healthy passes and broken/stalled/leak/missing fail.

## DAG

```
T-010-04-01  swap-proof-alternate-boundary  ← T-010-02-02, T-010-03-01
T-010-04-02  missing-contract-loud-and-verify  ← T-010-04-01
```

Wave rationale: Two tickets in series after both rewire stories land: the swap-proof ticket stands up the alternate declaration + stub and proves healthy-pass / fault-fail; the loud-missing-contract + npm run verify ticket reuses that scaffolding and adds the final regression gate, so it depends on the swap-proof ticket.

**Out of this slice:** Making the alternate boundary a permanent template feature; a fleet-wide 'every demo ships a contract linter' gate (a separate epic, per the epic's right-size note); changing the receipt boundary's behavior; any provider-abstraction layer (N2 — not an all-provider abstraction layer).

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-12._
