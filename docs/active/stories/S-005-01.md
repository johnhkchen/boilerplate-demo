---
id: S-005-01
title: self-labeling-front-door
type: story
status: open
priority: high
tickets: [T-005-01-01, T-005-01-02, T-005-01-03]
---

**Scope:** src/pages/index.astro end to end — the identity card, the signed-note card, one new primary-action affordance, and a visible route to /backstage — plus the template's name/primary-action slots as frontmatter consts. The Playwright surface it must keep honest lives in tests/demo-flow.spec.ts and tests/support/flow-contract.ts (a new labeled-action assertion lands there). backstage.astro, all of src/lib/*, and the clay tokens/primitives are read-only context, not touched.

**Story acceptance:** Opening the built index cold shows every section carrying a verb-forward label, exactly one clay-button primary action, and a discoverable link to backstage; `npx playwright test` passes end to end including a new assertion that the labeled primary action exists and responds within the flow budget; the leak check still passes on the public page; and the projector/phone pass artifact in docs/active/work/ answers 'what is this page, what is its one action, how do I leave the team a note' from the screenshots alone.

**Honest boundary:** Copy stays template placeholder with obvious slots — the generated project's real name and primary action come from the idea (N1 — not a universal application framework), named here and not pre-baked. The Playwright assertion proves the labeled action exists and responds; it does not claim the page is convincing (N4 — not automated theater). The projector/phone pass is the implementing session's own cold-read proxy — a true never-seen-it human reading the page is the epic's human gate, deferred to the person at the counter, not simulated by a check.

## DAG

```
T-005-01-01  verb-forward-index-recomposition
T-005-01-02  playwright-labeled-action-assertion  ← T-005-01-01
T-005-01-03  projector-and-phone-cold-read-pass  ← T-005-01-01
```

Wave rationale: T-005-01-01 runs alone: both other tickets read the recomposed page's settled labels and DOM hooks, so nothing can fan out until the surface stops moving. T-005-01-02 and T-005-01-03 then run in parallel safely — one writes only under tests/, the other writes only a work artifact under docs/active/work/T-005-01-03/, disjoint files with no shared state.

**Out of this slice:** Restyling the clay kit, tokens, or b28-clay primitives; any change to backstage's copy, auth, or storage (its labels are already verb-forward); per-demo copywriting; CMS or content tooling (N5 — not framework-by-inertia); portfolio/fleet surfaces (P7 — fleet support stays optional); rewriting the receipt API or any src/lib module.

---
_Materialized by Vend's `decompose-epic` play — 3 ticket(s), 2026-07-11._
