---
id: S-009-01
title: binding-voice-standard-and-loop-wiring
type: story
status: open
priority: high
tickets: [T-009-01-01, T-009-01-02]
---

**Scope:** A new standard doc at docs/knowledge/copy-voice-standard.md and its wiring onto the loop's authoring read-path — the injected docs/knowledge/rdspi-workflow.md plus the CLAUDE.md/AGENTS.md knowledge pointers. No src/ surface changes. The brand 'parlor, not portfolio' rules and the b28-clay kit are the source voice, read-only, distilled into a checkable contract rather than re-invented.

**Story acceptance:** docs/knowledge/copy-voice-standard.md exists stating a concrete envelope (plain kitchen-table English, a brevity/length ceiling per surface element, names-as-wayfinding, verb-forward labels), and it is referenced from the injected authoring path so a fresh RDSPI session has the standard in context — verifiable by the pointer resolving to the file and by the S-009-02 sweep sessions citing the standard in their Research artifacts.

**Honest boundary:** The doc is a portable in-repo contract read by the loop, not a SaaS control plane (N3 — not a mandatory SaaS control plane) and not an automated gate — a machine-checkable length/voice check across the whole fleet is the deferred separate epic the right-size names, not built here. No new injection machinery: the standard rides the existing seam that injects rdspi-workflow.md; the epic depends on that path already existing (per CLAUDE.md), which it does.

## DAG

```
T-009-01-01  author-voice-length-standard-doc
T-009-01-02  wire-standard-into-authoring-read-path  ← T-009-01-01
```

Wave rationale: T-009-01-01 authors the doc alone — the wire needs the doc's settled path and section names before a reference can resolve. T-009-01-02 wires it after, pointing at that settled file. Serial by necessity, not choice; both are small doc-only edits.

**Out of this slice:** Rewriting any src/ copy (that is S-009-02); inventing a new voice (the existing brand rules and b28-clay kit already bind — this only makes them enforceable); building an automated length/voice gate or any fleet-wide enforcement (separate epic); restyling the clay kit or tokens.

---
_Materialized by Vend's `decompose-epic` play — 2 ticket(s), 2026-07-11._
